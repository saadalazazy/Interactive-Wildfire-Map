**اختبار عملي – نظام تفاعلي لحرائق الغابات**

**الهدف**

المطلوب تطوير صفحة ويب بسيطة باستخدام JavaScript لعرض بيانات حرائق الغابات على خريطة تفاعلية، مع إمكانية:

1. عرض الحرائق على الخريطة.
2. الضغط على أي حريق وعرض بياناته في Popup.
3. اختيار حريق أو أكثر من الخريطة.
4. حذف المعالم المحددة من العرض على الخريطة.
5. إرسال المعالم المحددة إلى C# Web API. حفظ البيانات المرسلة في ملف JSON على السيرفر.

**مصدر بيانات الحرائق**

استخدم طبقة الحرائق التالية:

\[ArcGIS Wildfire FeatureServer\]

<https://sampleserver6.arcgisonline.com/arcgis/rest/services/Wildfire/FeatureServer>

**المطلوب**

1\. عرض الخريطة والحرائق

إنشاء صفحة HTML تحتوي على خريطة تفاعلية باستخدام:

ArcGIS Maps SDK for JavaScript

ثم إضافة طبقة الـ Wildfire من الرابط السابق وعرض المعالم على الخريطة.

يجب أن تكون الخريطة قابلة للتكبير والتحريك.

**2\. عرض Popup**

عند الضغط على أي حريق في الخريطة:

يتم تحديد الحريق.

يظهر Popup.

يعرض الـ Popup البيانات الخاصة بالمعلم، مثل الـ ID والحقول المتاحة في الطبقة.

لا يشترط عرض جميع الحقول، ويمكن اختيار الحقول المهمة.

**3\. اختيار وحذف الحرائق**

أضف طريقة تسمح للمستخدم باختيار حريق واحد أو أكثر من الخريطة.

يجب أن يكون هناك زر:

Delete Selected

عند الضغط عليه:

يتم حذف المعالم المحددة من عرض الخريطة فقط.

لا يجب حذفها من ArcGIS FeatureServer الأصلي.

يمكن استخدام \`FeatureLayerView\` أو أي طريقة مناسبة لتنفيذ ذلك.

**4\. إنشاء C# Web API**

إنشاء مشروع ASP.NET Core Web API يحتوي على Endpoint من نوع:

\`POST\`

مثلاً:

\`POST /api/fires\`

يستقبل البيانات التي تم اختيارها من الخريطة.

مثال مبسط للبيانات المرسلة:

json

\[

{

"id": 1,

"attributes": {

"OBJECTID": 1,

"FIRE_NAME": "Fire 1"

},

"geometry": {

"x": -117.1,

"y": 34.2

}

}

\]

5\. حفظ البيانات في JSON

عند استقبال البيانات من الـ Frontend:

يتم حفظها في ملف:

\`saved_fires.json\`

إذا كان الملف يحتوي على بيانات سابقة، يجب إضافة البيانات الجديدة إليها وعدم حذف البيانات القديمة.

مثال:

\`\`\`json

\[

{

"id": 1,

"name": "Fire 1"

},

{

"id": 2,

"name": "Fire 2"

}

\]

6\. ربط JavaScript بالـ API

إضافة زر:

Save Selected

عند الضغط عليه:

1\. الحصول على المعالم التي قام المستخدم باختيارها.

2\. تجهيز البيانات.

3\. إرسالها إلى C API باستخدام:

\`fetch\`

مع استخدام:

\`async / await\`

مثال:

\`\`\`javascript

const response = await fetch("<https://localhost:xxxx/api/fires>", {

method: "POST",

headers: {

"Content-Type": "application/json"

},

body: JSON.stringify(selectedFires)

});\`\`\`

بعد نجاح العملية تظهر رسالة للمستخدم مثل:

تم حفظ البيانات بنجاح.

النتيجة النهائية

يجب أن يكون التطبيق قادراً على تنفيذ السيناريو التالي:

1. فتح الصفحة
2. ظهور الخريطة والحرائق
3. الضغط على حريق
4. ظهور الـ Popup
5. اختيار حريق أو أكثر
6. حذف الحرائق المحددة من العرض أو حفظها في السيرفر
7. إرسال البيانات إلى C# API
8. حفظ البيانات في saved_fires.json

📦 المطلوب تسليمه

1\. ملف HTML / CSS / JavaScript للواجهة الأمامية.

2\. مشروع C ASP.NET Core Web API.

3\. ملف \`saved_fires.json\`.

4\. README بسيط يوضح:

طريقة تشغيل الـ Frontend.

طريقة تشغيل الـ API.

رابط الـ API المستخدم.

5\. فيديو قصير أو شرح مختصر يوضح تشغيل الوظائف الأساسية.

ملاحظات

استخدم JavaScript

الباك اند استخدم C# in web api

يمكنك استخدام أي طريقة مناسبة في ArcGIS Maps SDK لتنفيذ الـ Select والـ Popup.

المطلوب التركيز على صحة التنفيذ وفهم طريقة الربط بين الخريطة والـ API أكثر